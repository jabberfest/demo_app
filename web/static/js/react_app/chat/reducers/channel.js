// Reducer
const channels = (state = [], action) => {
    switch (action.type) {
        case 'ADD_CHANNEL':
            debugger;
            return state;
        default:
            return state;
    }
}


export default channels