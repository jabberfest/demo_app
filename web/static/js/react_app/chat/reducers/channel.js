// Reducer
const channels = (state = [1,2,3,4], action) => {
    switch (action.type) {
        case 'ADD_CHANNEL':
            debugger;
            return state;
        default:
            return state;
    }
}


export default channels